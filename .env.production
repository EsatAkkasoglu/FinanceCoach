# Production build env — consumed by Vite at `pnpm build`.
# /api is rewritten to the Cloud Run service `fincoach-backend` (europe-west1)
# by firebase.json. Single origin, no CORS, no secret here.
VITE_API_BASE=/api
